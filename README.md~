# C3p0TestDemo
使用C3p0连接池操作MySQL数据库

	1 添加两个JAR包： c3p0.jar和mysql-connector-java.jar;
	2 使用MySQL数据库:数据库名db,user:root,password:123456(根据MySQL安装的用户名和密码自行修改项目中c3p0.properties文件内容);
	3 数据库名称为db,表名为users,字段名：id,name,password(根据自己建表的情况进行修改);
	4 预先添加数据，并在代码中根据具体情况修改参数。
